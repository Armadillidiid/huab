{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "huab",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "bauh": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run src/bin.ts --watch",
    "build": "bun run ./scripts/build-binaries.ts",
    "postbuild": "bun run scripts/inject-constant.ts",
    "clean": "rm -rf dist/*",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "format": "echo 'use oxc",
    "lint": "echo 'use oxc'"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^25.2.3",
    "@types/yargs": "^17.0.35",
    "@typescript/native-preview": "7.0.0-dev.20260216.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "yargs": "^18.0.0"
  }
}
