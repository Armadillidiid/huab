import { cmd } from "@/utils";

export const WebCommand = cmd({
  command: "web",
  describe: "open web interface",
  handler: async () => {
    // TODO: open web interface
    console.log("TODO: open web interface");
  },
});
